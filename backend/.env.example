# Copie este arquivo para .env e ajuste usuário, senha e nome do banco.
# Crie o banco vazio no PostgreSQL antes de rodar as migrations.
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/NOME_DB?schema=public"
